#include <stdio.h>
#include <stdlib.h>
#include <malloc.h>
#include <time.h>

typedef struct Node {
  int data;
  int* next;
  typedef struct* head;
} node;

void create_node(int data, int* next) {
  int* head = node.head;
  head = (int *)malloc(sizeof(int*));
  head -> node.next;



}



int main() {

  return 0;
}
